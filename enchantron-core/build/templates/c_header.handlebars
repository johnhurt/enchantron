#ifndef enchantron_h
#define enchantron_h

typedef struct UnboxedAny UnboxedAny;

{{c_header_imports}}

{{~#each types}}
{{~#if rust_owned}}{{~#unless exclude_from_header}}typedef struct {{name}} {{name}};
{{/unless}}{{/if}}
{{~/each}}

{{c_header_body}}

#endif /* enchantron_h */